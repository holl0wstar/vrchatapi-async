from setuptools import setup

NAME = 'VRChatAPI-Async'
VERSION = '0.0.1'

setup(
    name=NAME,
    version=VERSION,
    author='Skyelight',
    author_email='distortedinferno@gmail.com',
    description='An async version of the VRChat API Library.',
    long_description=description,
    python_requires='>=3.8',
)
